import { SAFE_GAME_HEIGHT, SAFE_GAME_WIDTH } from "../constants/viewport";

export class GameModel {
  static GAME_WIDTH = SAFE_GAME_WIDTH;
  static GAME_HEIGHT = SAFE_GAME_HEIGHT;
  static MAIN_SCENE = null;
}
